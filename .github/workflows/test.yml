name: Test

on:
  pull_request:

jobs:
  test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Setup vcpkg
      run: |
        cd vcpkg
        .\bootstrap-vcpkg.bat
        .\vcpkg.exe integrate install

    - name: Set VCPKG_ROOT environment variable
      run: |
        echo "VCPKG_ROOT=${{ github.workspace }}\vcpkg" >> $env:GITHUB_ENV

    - name: Restore .NET dependencies
      run: dotnet restore

    - name: Build solution
      run: msbuild CaptureTool.slnx /p:Configuration=Release /p:Platform=x64

    - name: Run C++ native tests
      run: |
        $vsTestPath = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Workload.NativeDesktop -property installationPath
        $vsTestExe = Join-Path $vsTestPath "Common7\IDE\CommonExtensions\Microsoft\TestWindow\vstest.console.exe"
        & $vsTestExe "bin\Release\x64\CaptureInterop.Tests.dll" /Platform:x64

    - name: Run .NET tests
      run: |
        dotnet test src\CaptureTool.Core.Tests\CaptureTool.Core.Tests.csproj -p:Platform=x64 --no-build --configuration Release
        dotnet test src\CaptureTool.Services.Tests\CaptureTool.Services.Tests.csproj -p:Platform=x64 --no-build --configuration Release
        dotnet test src\CaptureTool.ViewModels.Tests\CaptureTool.ViewModels.Tests.csproj -p:Platform=x64 --no-build --configuration Release
        dotnet test src\CaptureTool.Domains.Edit.Tests\CaptureTool.Domains.Edit.Tests.csproj -p:Platform=x64 --no-build --configuration Release
