name: .NET Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '9.0.306' # match your project

    - name: Restore dependencies
      run: dotnet restore

    - name: Build entire solution (including C++ projects)
      run: msbuild CaptureTool.sln /p:Configuration=Release /m

    - name: Run C# tests
      run: dotnet test "src/**/*.csproj" --no-build --verbosity normal