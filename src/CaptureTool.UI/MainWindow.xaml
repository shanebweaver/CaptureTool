<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="CaptureTool.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:CaptureTool.UI.Xaml.Pages"
    xmlns:views="using:CaptureTool.UI.Xaml.Views"
    Title="CaptureTool"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <views:AppTitleBarView />

        <Grid Row="1">
            <views:AppMenuView />
        </Grid>

        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Row="2">
            <Frame x:Name="NavigationFrame" SourcePageType="pages:LoadingPage" />
        </Grid>
    </Grid>
</Window>
