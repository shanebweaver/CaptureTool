<?xml version="1.0" encoding="utf-8" ?>
<local:ImageEditPageBase
    x:Class="CaptureTool.UI.Xaml.Pages.ImageEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:CaptureTool.UI.Xaml.Views.ImageEditCanvas"
    xmlns:canvasui="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CaptureTool.ViewModels"
    xmlns:views="using:CaptureTool.UI.Xaml.Views"
    d:DataContext="{d:DesignInstance Type=viewmodels:ImageEditPageViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  Image  -->
        <DataTemplate x:Key="ImageCanvasItemDataTemplate" x:DataType="viewmodels:ImageCanvasItemViewModel">
            <canvas:ImageCanvasItemView ViewModel="{x:Bind Mode=OneWay}">
                <UserControl.RenderTransform>
                    <CompositeTransform TranslateX="{x:Bind Left, Mode=OneWay}" TranslateY="{x:Bind Top, Mode=OneWay}" />
                </UserControl.RenderTransform>
            </canvas:ImageCanvasItemView>
        </DataTemplate>

        <!--  Rectangle  -->
        <DataTemplate x:Key="RectangleCanvasItemDataTemplate" x:DataType="viewmodels:RectangleCanvasItemViewModel">
            <canvas:RectangleCanvasItemView ViewModel="{x:Bind Mode=OneWay}">
                <UserControl.RenderTransform>
                    <CompositeTransform TranslateX="{x:Bind Left, Mode=OneWay}" TranslateY="{x:Bind Top, Mode=OneWay}" />
                </UserControl.RenderTransform>
            </canvas:RectangleCanvasItemView>
        </DataTemplate>

        <!--  Text  -->
        <DataTemplate x:Key="TextCanvasItemDataTemplate" x:DataType="viewmodels:TextCanvasItemViewModel">
            <canvas:TextCanvasItemView ViewModel="{x:Bind Mode=OneWay}">
                <UserControl.RenderTransform>
                    <CompositeTransform TranslateX="{x:Bind Left, Mode=OneWay}" TranslateY="{x:Bind Top, Mode=OneWay}" />
                </UserControl.RenderTransform>
            </canvas:TextCanvasItemView>
        </DataTemplate>

        <!--  Template selector  -->
        <local:CanvasItemTemplateSelector
            x:Key="CanvasItemTemplateSelector"
            ImageTemplate="{StaticResource ImageCanvasItemDataTemplate}"
            RectangleTemplate="{StaticResource RectangleCanvasItemDataTemplate}"
            TextTemplate="{StaticResource TextCanvasItemDataTemplate}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right" IsOpen="False">
            <AppBarButton
                Command="{x:Bind ViewModel.CropCommand}"
                Icon="Crop"
                IsEnabled="False"
                Label="Crop" />
            <AppBarButton
                Command="{x:Bind ViewModel.CopyCommand}"
                Icon="Copy"
                IsEnabled="False"
                Label="Copy" />
            <AppBarButton
                Command="{x:Bind ViewModel.SaveCommand}"
                Icon="Save"
                IsEnabled="False"
                Label="Save" />
        </CommandBar>

        <Grid
            x:Name="CanvasContainer"
            Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}"
            Row="1">
            <ScrollView
                x:Name="CanvasScrollView"
                HorizontalScrollBarVisibility="Auto"
                ZoomMode="Enabled">
                <canvasui:CanvasControl
                    x:Name="AnnotationCanvas"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    CreateResources="CanvasControl_CreateResources"
                    Draw="CanvasControl_Draw" />
            </ScrollView>
        </Grid>
    </Grid>
</local:ImageEditPageBase>
