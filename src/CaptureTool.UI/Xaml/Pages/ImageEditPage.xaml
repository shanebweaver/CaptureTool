<?xml version="1.0" encoding="utf-8" ?>
<local:ImageEditPageBase
    x:Class="CaptureTool.UI.Xaml.Pages.ImageEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CaptureTool.ViewModels"
    xmlns:views="using:CaptureTool.UI.Xaml.Views"
    d:DataContext="{d:DesignInstance Type=viewmodels:ImageEditPageViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  Image  -->
        <DataTemplate x:Key="ImageCanvasItemDataTemplate" x:DataType="viewmodels:ImageCanvasItemViewModel">
            <views:ImageCanvasItemView ViewModel="{x:Bind Mode=OneWay}">
                <views:ImageCanvasItemView.RenderTransform>
                    <CompositeTransform TranslateX="{x:Bind Left, Mode=OneWay}" TranslateY="{x:Bind Top, Mode=OneWay}" />
                </views:ImageCanvasItemView.RenderTransform>
            </views:ImageCanvasItemView>
        </DataTemplate>

        <!--  Rectangle  -->
        <DataTemplate x:Key="RectangleCanvasItemDataTemplate" x:DataType="viewmodels:RectangleCanvasItemViewModel">
            <!--  TODO: Create separate view and add render transform  -->
            <Rectangle Width="{x:Bind Width, Mode=OneWay}" Height="{x:Bind Height, Mode=OneWay}" />
        </DataTemplate>

        <!--  Text  -->
        <DataTemplate x:Key="TextCanvasItemDataTemplate" x:DataType="viewmodels:TextCanvasItemViewModel">
            <!--  TODO: Create separate view and add render transform  -->
            <TextBlock Text="{x:Bind Text, Mode=OneWay}" />
        </DataTemplate>

        <local:CanvasItemTemplateSelector
            x:Key="CanvasItemTemplateSelector"
            ImageTemplate="{StaticResource ImageCanvasItemDataTemplate}"
            RectangleTemplate="{StaticResource RectangleCanvasItemDataTemplate}"
            TextTemplate="{StaticResource TextCanvasItemDataTemplate}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right" IsOpen="False">
            <AppBarButton
                Icon="Crop"
                IsEnabled="False"
                Label="Crop" />
            <AppBarButton
                Icon="Copy"
                IsEnabled="False"
                Label="Copy" />
            <AppBarButton
                Icon="Save"
                IsEnabled="False"
                Label="Save" />
        </CommandBar>

        <Grid Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}" Row="1">
            <ItemsControl ItemTemplateSelector="{StaticResource CanvasItemTemplateSelector}" ItemsSource="{x:Bind ViewModel.CanvasItems, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Grid>
</local:ImageEditPageBase>
