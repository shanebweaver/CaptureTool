<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="CaptureTool.UI.Xaml.Windows.DesktopImageCaptureWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CaptureTool.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <controls:SelectionOverlay SelectionRect="{x:Bind ViewModel.CaptureArea, Mode=TwoWay}" />

        <Grid
            Margin="0,12,0,0"
            Padding="8,4"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            CornerRadius="8">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12">

                <Button>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <SymbolIcon VerticalAlignment="Center" Symbol="Camera" />
                        <TextBlock VerticalAlignment="Center" Text="Capture" />
                    </StackPanel>
                </Button>

                <ToggleSwitch MinWidth="0">
                    <ToggleSwitch.OnContent>
                        <SymbolIcon Symbol="Camera" />
                    </ToggleSwitch.OnContent>
                    <ToggleSwitch.OffContent>
                        <SymbolIcon Symbol="Video" />
                    </ToggleSwitch.OffContent>
                </ToggleSwitch>

                <ToggleButton
                    Width="32"
                    Height="32"
                    Padding="0"
                    IsChecked="{x:Bind ViewModel.ShowOptions, Mode=TwoWay}">
                    <SymbolIcon Symbol="More" />
                </ToggleButton>

                <Button
                    Width="32"
                    Height="32"
                    Padding="0"
                    Command="{x:Bind ViewModel.GoBackCommand}">
                    <SymbolIcon Symbol="Clear" />
                </Button>
            </StackPanel>

            <Grid
                Padding="0,12"
                Row="1"
                Visibility="{x:Bind ViewModel.ShowOptions, Mode=OneWay}">
                <TextBlock Text="Options" />
            </Grid>
        </Grid>
    </Grid>

</Window>
