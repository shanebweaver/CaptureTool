<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="CaptureTool.UI.Xaml.Windows.DesktopImageCaptureWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CaptureTool.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <controls:CropOverlay CropRect="{x:Bind ViewModel.CaptureArea, Mode=TwoWay}" />

        <Grid
            Padding="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="Red">
            <Button Command="{x:Bind ViewModel.GoBackCommand}">
                <TextBlock Text="Go back" />
            </Button>
        </Grid>
    </Grid>

</Window>
