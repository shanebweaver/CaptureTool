<?xml version="1.0" encoding="utf-8" ?>
<local:AppMenuViewBase
    x:Class="CaptureTool.UI.Xaml.Views.AppMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CaptureTool.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:AppMenuViewModel}"
    mc:Ignorable="d">

    <MenuBar>
        <!--  File  -->
        <MenuBarItem Title="File">
            <!--  New capture  -->
            <MenuFlyoutSubItem Icon="Add" Text="New capture">
                <MenuFlyoutItem Icon="Camera" Text="Screenshot" />
                <MenuFlyoutItem Icon="Video" Text="Video" />
                <MenuFlyoutItem Icon="Microphone" Text="Audio" />
            </MenuFlyoutSubItem>

            <!--  Open  -->
            <MenuFlyoutItem Icon="Folder" Text="Open...">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="O" Modifiers="Control" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
            <MenuFlyoutSeparator />

            <!--  Settings  -->
            <MenuFlyoutItem
                Command="{x:Bind ViewModel.GoToSettingsCommand}"
                Icon="Setting"
                Text="Settings" />

            <!--  Exit  -->
            <MenuFlyoutItem Text="Exit">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F4" Modifiers="Menu" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
        </MenuBarItem>

        <!--  Help  -->
        <MenuBarItem Title="Help">
            <MenuFlyoutItem Text="About" />
        </MenuBarItem>
    </MenuBar>
</local:AppMenuViewBase>
