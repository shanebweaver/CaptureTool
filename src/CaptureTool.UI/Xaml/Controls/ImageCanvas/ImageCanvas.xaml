<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="CaptureTool.UI.Xaml.Controls.ImageCanvas.ImageCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvasui="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Xaml.Controls.ImageCanvas"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}"
    mc:Ignorable="d">
    <Grid
        x:Name="RootContainer"
        PointerCanceled="RootContainer_PointerCanceled"
        PointerCaptureLost="RootContainer_PointerCaptureLost"
        PointerMoved="RootContainer_PointerMoved"
        PointerPressed="RootContainer_PointerPressed"
        PointerReleased="RootContainer_PointerReleased"
        SizeChanged="RootContainer_SizeChanged">

        <ScrollView
            x:Name="CanvasScrollView"
            HorizontalScrollBarVisibility="Auto"
            ZoomMode="Enabled">
            <Grid
                x:Name="CanvasContainer"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <canvasui:CanvasControl
                    x:Name="RenderCanvas"
                    CreateResources="CanvasControl_CreateResources"
                    Draw="CanvasControl_Draw" />

                <Canvas x:Name="CropCanvas">
                    <Grid x:Name="CropBoundary" Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Border  -->
                        <Rectangle
                            Grid.RowSpan="5"
                            Grid.ColumnSpan="5"
                            Margin="-3"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Stroke="White"
                            StrokeThickness="1" />

                        <!--  Crop anchors  -->
                        <!--  Top left  -->
                        <Polygon
                            x:Name="CropAnchor_TopLeft"
                            Fill="White"
                            IsTabStop="True"
                            Points="0,0 40,0 40,8, 8,8 8,40, 0,40"
                            Stroke="Black"
                            UseSystemFocusVisuals="True"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="-9" Y="-9" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Top  -->
                        <Polygon
                            x:Name="CropAnchor_Top"
                            Grid.Column="2"
                            Fill="White"
                            IsTabStop="True"
                            Points="0,0 40,0 40,8 0,8"
                            Stroke="Black"
                            UseSystemFocusVisuals="True"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform Y="-9" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Top right  -->
                        <Polygon
                            x:Name="CropAnchor_TopRight"
                            Grid.Column="4"
                            Fill="White"
                            Points="0,0 40,0 40,40 32,40 32,8 0,8"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="10" Y="-9" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Left  -->
                        <Polygon
                            x:Name="CropAnchor_Left"
                            Grid.Row="2"
                            Fill="White"
                            Points="0,0 8,0 8,40 0,40"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="-9" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Right  -->
                        <Polygon
                            x:Name="CropAnchor_Right"
                            Grid.Row="2"
                            Grid.Column="4"
                            Fill="White"
                            Points="32,0 40,0 40,40 32,40"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="10" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Bottom left  -->
                        <Polygon
                            x:Name="CropAnchor_BottomLeft"
                            Grid.Row="4"
                            Fill="White"
                            Points="0,0 8,0 8,32 40,32 40,40 0,40"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="-9" Y="9" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Bottom  -->
                        <Polygon
                            x:Name="CropAnchor_Bottom"
                            Grid.Row="4"
                            Grid.Column="2"
                            Fill="White"
                            Points="0,32 40,32 40,40 0,40"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform Y="10" />
                            </Polygon.RenderTransform>
                        </Polygon>
                        <!--  Bottom right  -->
                        <Polygon
                            x:Name="CropAnchor_BottomRight"
                            Grid.Row="4"
                            Grid.Column="4"
                            Fill="White"
                            Points="32,0 40,0 40,40 0,40 0,32 32,32"
                            Stroke="Black"
                            Visibility="{x:Bind IsCropModeEnabled, Mode=OneWay}">
                            <Polygon.RenderTransform>
                                <TranslateTransform X="10" Y="10" />
                            </Polygon.RenderTransform>
                        </Polygon>
                    </Grid>
                </Canvas>
            </Grid>
        </ScrollView>
    </Grid>
</UserControl>
