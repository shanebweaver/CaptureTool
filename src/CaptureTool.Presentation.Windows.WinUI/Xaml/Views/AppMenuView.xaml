<?xml version="1.0" encoding="utf-8" ?>
<local:AppMenuViewBase
    x:Class="CaptureTool.Presentation.Windows.WinUI.Xaml.Views.AppMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:CaptureTool.Presentation.Windows.WinUI.Helpers"
    xmlns:local="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CaptureTool.Application.Interfaces.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:IAppMenuViewModel}"
    Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}"
    mc:Ignorable="d">

    <MenuBar>
        <!--  File  -->
        <MenuBarItem x:Uid="AppMenu_FileMenuItem">
            <!--  New capture  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_NewCaptureItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.NewImageCaptureCommand)}"
                Icon="Camera">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator x:Uid="AppMenu_NewCaptureItem_KeyboardAccelerator" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>

            <!--  New video capture  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_NewVideoCaptureItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.NewVideoCaptureCommand)}"
                Icon="Video">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator x:Uid="AppMenu_NewVideoCaptureItem_KeyboardAccelerator" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>

            <!--  Open  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_OpenFileItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.OpenFileCommand)}"
                Icon="Folder">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator x:Uid="AppMenu_OpenFileItem_KeyboardAccelerator" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>

            <!--  Recent captures  -->
            <MenuFlyoutSubItem
                x:Name="RecentCapturesSubMenu"
                x:Uid="AppMenu_RecentCapturesItem"
                Icon="Clock" />

            <MenuFlyoutSeparator />

            <!--  Add-ons  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_AddOnsItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.ShowAddOnsCommand)}"
                Icon="Shop"
                IsEnabled="{x:Bind ViewModel.ShowAddOnsOption}"
                Visibility="{x:Bind ViewModel.ShowAddOnsOption}" />

            <!--  Settings  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_SettingsItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.NavigateToSettingsCommand)}"
                Icon="Setting" />

            <!--  Exit  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_ExitItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.ExitApplicationCommand)}"
                Icon="Cancel">
                <MenuFlyoutItem.KeyboardAccelerators>
                    <KeyboardAccelerator x:Uid="AppMenu_ExitItem_KeyboardAccelerator" />
                </MenuFlyoutItem.KeyboardAccelerators>
            </MenuFlyoutItem>
        </MenuBarItem>

        <!--  Help  -->
        <MenuBarItem x:Uid="AppMenu_HelpMenuItem">
            <!--  About  -->
            <MenuFlyoutItem
                x:Uid="AppMenu_AboutItem"
                Command="{x:Bind helpers:XamlCommandHelpers.ToICommand(ViewModel.ShowAboutAppCommand)}"
                Icon="Help" />
        </MenuBarItem>
    </MenuBar>
</local:AppMenuViewBase>
