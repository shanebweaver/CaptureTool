<?xml version="1.0" encoding="utf-8" ?>
<local:UserControlBase
    x:Class="CaptureTool.Presentation.Windows.WinUI.Xaml.Controls.CaptureOverlayToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:capture="using:CaptureTool.Domain.Capture"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:templateselectors="using:CaptureTool.Presentation.Windows.WinUI.Xaml.TemplateSelectors"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:utils="using:CaptureTool.Presentation.Windows.WinUI.Utils"
    d:DataContext="{d:DesignInstance Type=local:CaptureOverlayToolbar}"
    mc:Ignorable="d">
    <Grid
        x:Name="ToolbarPanel"
        Padding="8"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
        CornerRadius="4">

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button
                x:Uid="CaptureOverlay_BackButton"
                Command="{x:Bind GoBackCommand, Mode=OneWay}"
                Style="{StaticResource IconButtonStyle}">
                <SymbolIcon Symbol="Back" />
            </Button>

            <Button
                x:Uid="CaptureOverlay_PauseResumeButton"
                Command="{x:Bind TogglePauseResumeCommand, Mode=OneWay}"
                Style="{StaticResource IconButtonStyle}"
                Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(IsRecording), Mode=OneWay}">
                <Grid>
                    <SymbolIcon Symbol="Play" Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(IsPaused), Mode=OneWay}" />
                    <SymbolIcon Symbol="Pause" Visibility="{x:Bind utils:VisibilityUtility.InverseBoolToVisibility(IsPaused), Mode=OneWay}" />
                </Grid>
            </Button>

            <Grid>
                <Button
                    x:Name="StartVideoCaptureButton"
                    x:Uid="CaptureOverlay_StartVideoCaptureButton"
                    MinWidth="100"
                    Background="#AAFF3340"
                    Command="{x:Bind StartVideoCaptureCommand, Mode=OneWay}"
                    IsEnabled="{x:Bind CanStartRecording, Mode=OneWay}"
                    Visibility="{x:Bind utils:VisibilityUtility.InverseBoolToVisibility(IsRecording), Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon
                            Margin="0,0,0,-2"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource FluentSystemIcons}"
                            FontSize="16"
                            Glyph="{StaticResource Record16Regular}" />
                        <TextBlock x:Uid="StartVideoCaptureButtonTextBlock" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button
                    x:Name="StopVideoCaptureButton"
                    x:Uid="CaptureOverlay_StopVideoCaptureButton"
                    MinWidth="100"
                    Background="#AAFF3340"
                    Command="{x:Bind StopVideoCaptureCommand, Mode=OneWay}"
                    Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(IsRecording), Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon
                            Margin="0,0,0,-2"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource FluentSystemIcons}"
                            FontSize="16"
                            Glyph="{StaticResource Record16Regular}" />
                        <TextBlock x:Uid="StopVideoCaptureButtonTextBlock" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <Grid
                x:Uid="CaptureOverlay_Timer"
                Margin="8,0"
                VerticalAlignment="Center">
                <local:ClockTimer Time="{x:Bind CaptureTime, Mode=OneWay}" />
            </Grid>

            <SplitButton
                x:Name="LocalAudioToggleButton"
                x:Uid="CaptureOverlay_LocalAudioToggleButton"
                Height="32"
                Padding="6,0"
                Background="Transparent"
                Click="LocalAudioToggleButton_Click">
                <SplitButton.Content>
                    <Grid Width="20">
                        <FontIcon
                            FontFamily="{StaticResource FluentSystemIcons}"
                            Glyph="{StaticResource DesktopSpeaker20Regular}"
                            Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(IsLocalAudioEnabled), Mode=OneWay}" />
                        <FontIcon
                            FontFamily="{StaticResource FluentSystemIcons}"
                            Glyph="{StaticResource DesktopSpeakerOff20Regular}"
                            Visibility="{x:Bind utils:VisibilityUtility.InverseBoolToVisibility(IsLocalAudioEnabled), Mode=OneWay}" />
                    </Grid>
                </SplitButton.Content>
                <SplitButton.Flyout>
                    <Flyout Placement="Bottom" ShouldConstrainToRootBounds="False">
                        <StackPanel Width="240" Spacing="8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    x:Uid="CaptureOverlay_LocalAudioVolumeLabel"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind FormatVolumePercentage(LocalAudioVolumePercentage), Mode=OneWay}" />
                            </Grid>
                            <Slider
                                x:Name="LocalAudioVolumeSlider"
                                x:Uid="CaptureOverlay_LocalAudioVolumeSlider"
                                LargeChange="10"
                                Maximum="100"
                                Minimum="0"
                                SmallChange="1"
                                StepFrequency="1"
                                ValueChanged="LocalAudioVolumeSlider_ValueChanged"
                                Value="{x:Bind LocalAudioVolumePercentage, Mode=OneWay}" />
                        </StackPanel>
                    </Flyout>
                </SplitButton.Flyout>
            </SplitButton>

            <local:AudioInputSelector
                AudioInputSources="{x:Bind AudioInputSources, Mode=OneWay}"
                IsAvailable="{x:Bind IsAudioInputSelectionAvailable, Mode=OneWay}"
                IsMuted="{x:Bind IsAudioInputMuted, Mode=OneWay}"
                SelectedAudioInputSource="{x:Bind SelectedAudioInputSource, Mode=OneWay}"
                SelectedAudioInputSourceIndex="{x:Bind SelectedAudioInputSourceIndex, Mode=OneWay}"
                SelectionChangedCommand="{x:Bind SelectAudioInputSourceCommand, Mode=OneWay}"
                ShowSelectedAudioInputName="False"
                ToggleMuteCommand="{x:Bind ToggleAudioInputMuteCommand, Mode=OneWay}" />

            <Button
                x:Uid="SelectionOverlay_CloseButton"
                AutomationProperties.AutomationId="CaptureOverlay_CloseButton"
                Command="{x:Bind CloseCommand, Mode=OneWay}"
                Style="{StaticResource IconButtonStyle}">
                <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{StaticResource Cancel}" />
            </Button>
        </StackPanel>
    </Grid>
</local:UserControlBase>
