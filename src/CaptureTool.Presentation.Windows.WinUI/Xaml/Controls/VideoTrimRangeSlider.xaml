<?xml version="1.0" encoding="utf-8" ?>
<local:UserControlBase
    x:Class="CaptureTool.Presentation.Windows.WinUI.Xaml.Controls.VideoTrimRangeSlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Controls"
    MinHeight="64">

    <Grid Padding="8,0">
        <Canvas
            x:Name="TimelineCanvas"
            Height="48"
            MinWidth="120"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Background="Transparent"
            PointerPressed="TimelineCanvas_PointerPressed"
            SizeChanged="TimelineCanvas_SizeChanged">
            <Border
                x:Name="Track"
                Height="8"
                Background="{ThemeResource ControlStrongStrokeColorDefaultBrush}"
                CornerRadius="4"
                Opacity="0.45" />
            <Border
                x:Name="SelectedRange"
                Height="8"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                CornerRadius="4" />
            <Border
                x:Name="StartThumb"
                x:Uid="VideoTrimRangeSlider_StartThumb"
                Width="14"
                Height="34"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="3"
                IsTabStop="True"
                KeyDown="StartThumb_KeyDown"
                PointerCanceled="Thumb_PointerReleased"
                PointerEntered="Thumb_PointerEntered"
                PointerExited="Thumb_PointerExited"
                PointerMoved="Thumb_PointerMoved"
                PointerPressed="StartThumb_PointerPressed"
                PointerReleased="Thumb_PointerReleased"
                UseSystemFocusVisuals="True" />
            <Border
                x:Name="EndThumb"
                x:Uid="VideoTrimRangeSlider_EndThumb"
                Width="14"
                Height="34"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="3"
                IsTabStop="True"
                KeyDown="EndThumb_KeyDown"
                PointerCanceled="Thumb_PointerReleased"
                PointerEntered="Thumb_PointerEntered"
                PointerExited="Thumb_PointerExited"
                PointerMoved="Thumb_PointerMoved"
                PointerPressed="EndThumb_PointerPressed"
                PointerReleased="Thumb_PointerReleased"
                UseSystemFocusVisuals="True" />
            <Border
                x:Name="Playhead"
                x:Uid="VideoTrimRangeSlider_Playhead"
                Width="4"
                Height="44"
                Background="{ThemeResource TextFillColorPrimaryBrush}"
                CornerRadius="2"
                IsTabStop="True"
                KeyDown="Playhead_KeyDown"
                PointerCanceled="Thumb_PointerReleased"
                PointerEntered="Thumb_PointerEntered"
                PointerExited="Thumb_PointerExited"
                PointerMoved="Thumb_PointerMoved"
                PointerPressed="Playhead_PointerPressed"
                PointerReleased="Thumb_PointerReleased"
                UseSystemFocusVisuals="True" />
        </Canvas>
    </Grid>
</local:UserControlBase>
