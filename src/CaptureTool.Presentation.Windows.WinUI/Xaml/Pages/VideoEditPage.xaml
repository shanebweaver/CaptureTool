<?xml version="1.0" encoding="utf-8" ?>
<local:VideoEditPageBase
    x:Class="CaptureTool.Presentation.Windows.WinUI.Xaml.Pages.VideoEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="using:CaptureTool.Presentation.Windows.WinUI.Utils"
    xmlns:videoedit="using:CaptureTool.Presentation.Features.VideoEdit"
    d:DataContext="{d:DesignInstance Type=videoedit:VideoEditPageViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Toolbar  -->
        <CommandBar
            HorizontalAlignment="Stretch"
            DefaultLabelPosition="Right"
            IsOpen="False">
            <!--  Trim  -->
            <AppBarToggleButton
                x:Uid="VideoEdit_TrimButton"
                Command="{x:Bind ViewModel.ToggleTrimModeCommand}"
                Icon="Trim"
                IsChecked="{x:Bind ViewModel.IsInTrimMode, Mode=OneWay}"
                IsEnabled="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}" />

            <AppBarToggleButton
                x:Uid="VideoEdit_SuperResolutionButton"
                AutomationProperties.AutomationId="VideoEdit_SuperResolutionButton"
                Command="{x:Bind ViewModel.ToggleVideoSuperResolutionCommand}"
                IsChecked="{x:Bind ViewModel.IsVideoSuperResolutionActive, Mode=OneWay}"
                IsEnabled="{x:Bind ViewModel.IsVideoSuperResolutionAvailable, Mode=OneWay}"
                Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.IsVideoSuperResolutionFeatureEnabled), Mode=OneWay}">
                <AppBarToggleButton.Icon>
                    <FontIcon Glyph="&#xE945;" />
                </AppBarToggleButton.Icon>
            </AppBarToggleButton>

            <AppBarSeparator />

            <!--  Edit in Clipchamp  -->
            <AppBarButton
                x:Uid="VideoEdit_EditInClipchampButton"
                Command="{x:Bind ViewModel.EditInClipchampCommand}"
                Icon="Edit"
                IsEnabled="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}">
                <AppBarButton.Label>Edit in Clipchamp</AppBarButton.Label>
            </AppBarButton>

            <!--  Copy  -->
            <AppBarButton
                x:Uid="VideoEdit_CopyButton"
                Command="{x:Bind ViewModel.CopyCommand}"
                Icon="Copy"
                IsEnabled="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="C" Modifiers="Control" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>

            <!--  Save  -->
            <AppBarButton
                x:Uid="VideoEdit_SaveButton"
                Command="{x:Bind ViewModel.SaveCommand}"
                Icon="Save"
                IsEnabled="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}">
                <AppBarButton.Label>Save as</AppBarButton.Label>
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="S" Modifiers="Control" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>

            <!--  Open screen recordings folder  -->
            <AppBarButton
                x:Uid="VideoEdit_OpenVideosFolderButton"
                Command="{x:Bind ViewModel.OpenVideosFolderCommand}"
                Icon="Folder" />
        </CommandBar>

        <!--  Content  -->
        <Grid Grid.Row="1" Padding="8,0,8,8">
            <MediaPlayerElement
                x:Name="OriginalVideoPlayer"
                AreTransportControlsEnabled="{x:Bind ViewModel.AreTransportControlsVisible, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}" />
            <MediaPlayerElement
                x:Name="RenderedTrimVideoPlayer"
                AreTransportControlsEnabled="{x:Bind ViewModel.AreTransportControlsVisible, Mode=OneWay}"
                Visibility="Collapsed" />

            <Grid
                x:Name="TrimPreviewLoadingOverlay"
                Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                Visibility="Collapsed">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
                    <ProgressRing
                        Width="48"
                        Height="48"
                        IsActive="True" />
                    <TextBlock
                        x:Uid="VideoEdit_RenderingTrimPreviewText"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Opacity="0.7"
                        Text="Preparing preview..." />
                </StackPanel>
            </Grid>

            <Grid Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}" Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.IsVideoSuperResolutionGenerating), Mode=OneWay}">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
                    <controls:AiLoadingIndicator
                        x:Uid="VideoEdit_SuperResolutionProgressRing"
                        IsActive="{x:Bind ViewModel.IsVideoSuperResolutionGenerating, Mode=OneWay}" />
                    <TextBlock
                        x:Uid="VideoEdit_SuperResolutionProgressText"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Opacity="0.7"
                        Text="Enhancing video..." />
                </StackPanel>
            </Grid>

            <!--  Loading indicator for video finalization  -->
            <Grid Visibility="{x:Bind ViewModel.IsFinalizingVideo, Mode=OneWay}">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
                    <ProgressRing
                        Width="48"
                        Height="48"
                        IsActive="True" />
                    <TextBlock
                        x:Uid="VideoEdit_FinalizingText"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Opacity="0.7"
                        Text="Finalizing video..." />
                </StackPanel>
            </Grid>

            <Grid Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.IsMediaLoading), Mode=OneWay}">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
                    <ProgressRing
                        Width="48"
                        Height="48"
                        IsActive="True" />
                    <TextBlock
                        x:Uid="VideoEdit_LoadingMediaText"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Opacity="0.7"
                        Text="Opening video..." />
                </StackPanel>
            </Grid>

            <Grid
                Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
                Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.HasMediaFailure), Mode=OneWay}">
                <StackPanel
                    MaxWidth="520"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
                    <FontIcon FontSize="32" Glyph="&#xE783;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Text="{x:Bind ViewModel.MediaFailureMessage, Mode=OneWay}"
                        TextAlignment="Center"
                        TextWrapping="Wrap" />
                    <StackPanel
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button
                            x:Uid="MediaFailure_RetryButton"
                            Command="{x:Bind ViewModel.RetryMediaCommand}"
                            Content="Try again"
                            Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.CanRetryMedia), Mode=OneWay}" />
                        <Button
                            x:Uid="VideoEdit_MediaFailureOpenFolderButton"
                            Command="{x:Bind ViewModel.OpenVideosFolderCommand}"
                            Content="Open screen recordings folder" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid
            Grid.Row="2"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            BorderThickness="0,1,0,0"
            Visibility="{x:Bind ViewModel.IsInTrimMode, Mode=OneWay}">

            <Grid
                Grid.Row="2"
                MinHeight="96"
                MaxWidth="800"
                Padding="12,8"
                HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="56" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Uid="VideoEdit_TrimStartTimeText"
                        MinWidth="56"
                        FontSize="12"
                        Text="{x:Bind FormatTime(ViewModel.TrimStartSeconds), Mode=OneWay}" />
                    <TextBlock
                        x:Uid="VideoEdit_SelectedTrimDurationText"
                        Grid.Column="1"
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Text="{x:Bind FormatTrimDuration(ViewModel.TrimStartSeconds, ViewModel.TrimEndSeconds), Mode=OneWay}" />
                    <TextBlock
                        x:Uid="VideoEdit_TrimEndTimeText"
                        Grid.Column="2"
                        MinWidth="56"
                        FontSize="12"
                        Text="{x:Bind FormatTime(ViewModel.TrimEndSeconds), Mode=OneWay}"
                        TextAlignment="Right" />
                </Grid>

                <Button
                    x:Name="TrimPreviewPlayButton"
                    x:Uid="VideoEdit_TrimPreviewPlayButton"
                    Grid.Row="1"
                    Grid.Column="0"
                    Width="40"
                    Height="40"
                    Margin="0,0,12,0"
                    Padding="0"
                    VerticalAlignment="Center"
                    Click="TrimPreviewPlayButton_Click"
                    IsEnabled="{x:Bind ViewModel.IsMediaReady, Mode=OneWay}">
                    <FontIcon x:Name="TrimPreviewPlayIcon" Glyph="&#xE768;" />
                </Button>

                <Grid
                    Grid.Row="1"
                    Grid.Column="1"
                    ColumnSpacing="4">
                    <controls:VideoTrimRangeSlider
                        DurationSeconds="{x:Bind ViewModel.VideoDurationSeconds, Mode=OneWay}"
                        EndSeconds="{x:Bind ViewModel.TrimEndSeconds, Mode=OneWay}"
                        EndSecondsChanged="VideoTrimRangeSlider_EndSecondsChanged"
                        PlayheadSeconds="{x:Bind ViewModel.PlayheadSeconds, Mode=OneWay}"
                        PlayheadSecondsChanged="VideoTrimRangeSlider_PlayheadSecondsChanged"
                        StartSeconds="{x:Bind ViewModel.TrimStartSeconds, Mode=OneWay}"
                        StartSecondsChanged="VideoTrimRangeSlider_StartSecondsChanged" />
                </Grid>

                <TextBlock
                    x:Uid="VideoEdit_PlayheadTimeText"
                    Grid.Row="1"
                    Grid.Column="2"
                    MinWidth="56"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Text="{x:Bind FormatTime(ViewModel.PlayheadSeconds), Mode=OneWay}"
                    TextAlignment="Left" />
            </Grid>
        </Grid>
    </Grid>
</local:VideoEditPageBase>
