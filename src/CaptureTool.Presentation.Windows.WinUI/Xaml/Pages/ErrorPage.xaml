<?xml version="1.0" encoding="utf-8" ?>
<local:ErrorPageBase
    x:Class="CaptureTool.Presentation.Windows.WinUI.Xaml.Pages.ErrorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.Presentation.Windows.WinUI.Xaml.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shell="using:CaptureTool.Presentation.Shell"
    xmlns:utils="using:CaptureTool.Presentation.Windows.WinUI.Utils"
    d:DataContext="{d:DesignInstance Type=shell:ErrorPageViewModel}"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock
                x:Uid="ErrorPage_HeaderLabel"
                HorizontalAlignment="Center"
                FontSize="24"
                FontWeight="Bold"
                Foreground="Red"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                x:Uid="ErrorPage_RestartFailedLabel"
                MaxWidth="480"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                TextWrapping="Wrap"
                Visibility="{x:Bind utils:VisibilityUtility.BoolToVisibility(ViewModel.HasRestartFailed), Mode=OneWay}" />
            <StackPanel
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="8">
                <Button Command="{x:Bind ViewModel.RestartAppCommand}">
                    <TextBlock x:Uid="ErrorPage_RestartAppButtonLabel" />
                </Button>
                <Button Command="{x:Bind ViewModel.ExitAppCommand}">
                    <TextBlock x:Uid="ErrorPage_ExitAppButtonLabel" />
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</local:ErrorPageBase>
