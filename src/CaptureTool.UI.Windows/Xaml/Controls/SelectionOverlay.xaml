<?xml version="1.0" encoding="utf-8" ?>
<controls:UserControlBase
    x:Class="CaptureTool.UI.Windows.Xaml.Controls.SelectionOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CaptureTool.UI.Windows.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CaptureTool.UI.Windows.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=controls:SelectionOverlay}"
    IsTabStop="False"
    mc:Ignorable="d">
    <Canvas x:Name="SelectionCanvas" Background="Transparent">
        <ToolTipService.ToolTip>
            <ToolTip x:Name="SelectionToolTip" />
        </ToolTipService.ToolTip>
        <Grid x:Name="SelectionBoundary">
            <Grid.BorderBrush>
                <SolidColorBrush Opacity=".5" Color="Black" />
            </Grid.BorderBrush>

            <!--  Border  -->
            <Rectangle
                x:Name="SelectionRectangle"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Fill="Transparent"
                Stroke="White"
                StrokeThickness="2" />
        </Grid>
    </Canvas>
</controls:UserControlBase>
